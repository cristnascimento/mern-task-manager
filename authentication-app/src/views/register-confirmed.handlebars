<div class="d-flex col-12 justify-content-center">
  <div class="col-4 h-50 rounded mt-5 mb-5 border alert alert-success">
    {{#if err}}
        <div class="alert alert-danger">{{err}}</div>
    {{else}}
      <h3 class="mt-1">Parabéns!</h3>
        <hr>
      <p>Sua conta foi ativada com sucesso.</p>
      <p>Faça o <a href="/accounts/login">login</a> agora mesmo!</p>
    {{/if}}
  </div>
</div>